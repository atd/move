<h1><%= title %></h1>

<%= published_data(@audio) %>

<%= link_logo @audio, :size => 128, :url => formatted_audio_path(@audio, @audio.format) %>

<br />
<%= sanitize @audio.description %>

<p>
  <%= tags(@audio) %>
</p>

<hr>

<%= comments(@audio) %>
