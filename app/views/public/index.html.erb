<% atom_link_header :title => title(t('last activity'), :append_site_name => true) %>

<% content_for :title_header, title(t('last activity'), :append_site_name => true) %>
<% content_for :title_description, current_site.description %>
<% content_for :sidebar, public_menu %>
<% content_for :pagination, will_paginate(@contents) %>

<h1><%= t('last activity') %></h1>
<div id="activity_stream">
  <%= render :partial => 'content', :collection => @contents %>
</div>
