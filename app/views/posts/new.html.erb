<h1><%= t 'post.new' %></h1>
<% form_for([ @postable, @post ]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit t('post.submit') %>
  </p>
<% end %>
