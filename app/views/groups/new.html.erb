<h1><%= title %></h1>

<%= error_messages_for :group %>

<% form_for(@group, :html => { :multipart => true }) do |f| %>
  <%= render :partial => "form", :object => f %>
    
  <p>
    <%= submit_tag "Create".t %>
  </p>
<% end -%>
