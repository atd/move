<% atom_link_header(path_container, Article.new) %>

<% content_for :pagination, will_paginate(@articles) %>

<h1><%= title %></h1>
<div id="articles">
  <%= render :partial => "article", :collection => @articles %>
</div>
