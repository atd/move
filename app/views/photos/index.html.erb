<% atom_link_header(container, Photo.new) %>

<% content_for :sidebar, container_menu %>
<% content_for :pagination, will_paginate(@photos) %>

<h1><%= title %></h1>
<%= render :partial => 'photo', :collection => @photos %>
